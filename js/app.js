const jogos=['Aventura Pixel','Corrida Turbo','Pocket Quest','Arena 64'];let i=0;const b=document.getElementById('banner');function t(){b.textContent=jogos[i];i=(i+1)%jogos.length}t();setInterval(t,3000);const g=document.getElementById('games');jogos.forEach(n=>g.innerHTML+=`<div class='card'><h4>${n}</h4><button>Ver jogo</button></div>`);document.getElementById('q').oninput=e=>{const v=e.target.value.toLowerCase();[...g.children].forEach(c=>c.style.display=c.innerText.toLowerCase().includes(v)?'block':'none')};